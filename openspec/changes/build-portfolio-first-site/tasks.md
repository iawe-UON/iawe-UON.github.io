## 1. 测试基线与构建契约

- [ ] 1.1 使用 Node 内置测试运行器建立作品集构建测试入口，并让测试在隔离输出目录执行 Hexo 生成，避免依赖已提交的静态产物。
- [ ] 1.2 编写失败测试，断言生成结果必须包含 `/`、`/about/`、`/resume/`、`/achievements/`、`/blog/` 和 `/blog/page/2/`，且根路径不再是文章列表。
- [ ] 1.3 编写失败回归测试，记录并断言至少一个现有文章永久链接、`/tags/` 和 `/archives/` 在改造后仍然生成。
- [ ] 1.4 编写失败测试，断言作品集页面和博客列表不包含 Utterances 容器，而文章详情仍保留评论接入标记。
- [ ] 1.5 运行新增测试并确认它们因缺少新路由和模板而失败，保存失败原因作为实现基线。

## 2. 路由、导航与作品集数据

- [ ] 2.1 修改 Hexo 索引生成配置，将文章列表路径设置为 `blog`、布局设置为 `portfolio-blog`，保持每页十篇和日期倒序规则。
- [ ] 2.2 在 NexT 站点覆盖配置中定义首页、关于、简历、成果、博客菜单及中文菜单名称，并接入 `head`、`bodyEnd`、`style` 自定义文件路径。
- [ ] 2.3 创建根路径、关于、简历和成果页面源文件，分别指定对应作品集布局并显式关闭评论。
- [ ] 2.4 创建 `source/_data/portfolio.yml` 示例数据，完整覆盖 `profile`、`contact`、`resume`、三种成果类型以及可选字段，并将示例值清晰标记为待替换内容。
- [ ] 2.5 运行路由与兼容性测试，确认新路由生成、旧文章永久链接和归档路径保持通过。

## 3. 作品集共享模板与静态页面

- [ ] 3.1 编写失败的构建输出断言，覆盖首页内容顺序、最新文章最多三篇、无头像首字母降级以及缺少可选字段时不输出空元素。
- [ ] 3.2 创建作品集共享 Nunjucks macro，统一页面标题、卡片、标签、操作入口和空状态的语义结构。
- [ ] 3.3 实现 `portfolio-home` 布局，渲染个人定位、核心能力、前三项精选成果、最新三篇文章及简历/博客/联系入口。
- [ ] 3.4 实现 `portfolio-about` 布局，渲染简介、技术方向、经历摘要以及无按钮的静态邮箱和电话文本。
- [ ] 3.5 实现 `portfolio-resume` 布局，渲染工作、教育、技能和项目分区，并仅在配置 PDF 路径时输出下载入口。
- [ ] 3.6 运行静态页面测试，确认完整示例数据和缺失头像、PDF、邮箱、电话等降级场景均通过。

## 4. 浅色/深色主题与共享视觉系统

- [ ] 4.1 编写失败的主题脚本单元测试，覆盖无保存值默认浅色、有效深色恢复、无效值回退和本地存储异常降级。
- [ ] 4.2 在头部注入最小主题初始化脚本，在正文绘制前设置 `data-theme`，仅接受 `light` 或 `dark` 并以浅色为默认。
- [ ] 4.3 在页面尾部实现主题切换控件逻辑、可访问标签更新、本地持久化和 Utterances 明暗主题同步。
- [ ] 4.4 创建共享 Stylus 样式，以每套主题的背景、文字、强调三种基础颜色定义卡片、按钮、标签、焦点和统一圆角。
- [ ] 4.5 添加网格收缩、长中文与连续 URL 换行、代码块/表格/媒体宽度约束以及 `prefers-reduced-motion` 降级规则。
- [ ] 4.6 运行主题单元测试与静态构建，确认主题初始化、存储异常和样式资源接入全部通过。

## 5. 成果筛选与详情抽屉

- [ ] 5.1 编写失败的成果交互测试，覆盖全部/文章/软著/专利筛选、无结果状态、无效成果跳过和 `aria-pressed` 更新。
- [ ] 5.2 编写失败的抽屉交互测试，覆盖卡片打开、可选职责/外链隐藏、关闭按钮、遮罩、`Esc` 关闭和焦点恢复。
- [ ] 5.3 实现 `portfolio-achievements` 布局，以语义按钮渲染有效成果卡片、筛选器、每项成果详情 `<template>`、共享 `<dialog>` 和空状态。
- [ ] 5.4 实现原生 JavaScript 筛选状态和详情模板克隆逻辑，不输出证书、附件或图片预览，也不加载第三方交互库。
- [ ] 5.5 实现桌面右侧抽屉和移动底部抽屉样式，确保内容可滚动、尺寸不越过视口且减少动画偏好生效。
- [ ] 5.6 运行成果交互测试，确认筛选、空状态、抽屉关闭路径、焦点管理和可选字段降级全部通过。

## 6. 博客列表、标签与评论兼容

- [ ] 6.1 编写失败的博客输出断言，覆盖文章日期倒序、每页十篇、标题/日期/摘要/标签/详情链接和分页导航。
- [ ] 6.2 实现 `portfolio-blog` 布局，消费 Hexo 分页文章集合并输出圆角文章卡片及原生分页组件。
- [ ] 6.3 将文章卡片标签链接到 Hexo 原生标签归档，并验证 `/tags/` 和各标签归档不需要新增类别数据。
- [ ] 6.4 回归文章详情和 Utterances 配置，确认评论只在文章详情加载，手动主题切换时评论 iframe 使用匹配的 GitHub 明暗主题。
- [ ] 6.5 运行博客、标签、分页、文章永久链接、归档和评论回归测试并修复所有失败。

## 7. 响应式、可访问性与发布前验证

- [ ] 7.1 运行完整 `npm run build` 和全部自动化测试，确认无构建错误、丢失路由或新增生产依赖。
- [ ] 7.2 在 360×800、768×1024、1280×720 视口逐页验证 `/`、关于、简历、成果、博客、标签及一篇长文章，断言 `scrollWidth <= clientWidth`。
- [ ] 7.3 使用长中文标题、连续 URL、邮箱、电话、代码块、表格和媒体执行溢出回归，确认无文字覆盖、破损布局或整体横向滚动。
- [ ] 7.4 使用键盘完成导航、主题切换、成果筛选、抽屉打开和关闭，确认焦点可见、ARIA 状态正确且关闭后焦点返回触发项。
- [ ] 7.5 分别检查浅色与深色页面，确认站点自有界面仅使用三种基础色、所有指定组件为圆角矩形，第三方评论 iframe 仅作为明确例外。
- [ ] 7.6 在减少动态效果模式下复测主题和抽屉，确认非必要动画关闭但状态变化仍可识别。
- [ ] 7.7 检查示例个人资料和联系信息仍被清晰标记为占位内容，并在部署说明中提示发布前集中替换 `portfolio.yml`。
